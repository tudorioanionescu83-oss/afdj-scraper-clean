name: AFDJ Daily Scraper
on:
  workflow_dispatch:  # Manual RUN button ✅
  schedule:
    - cron: '30 9 * * *'  # 9:30 AM zilnic

jobs:
  scrape:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          
      - name
